import 'package:flutter/material.dart';

class CompleteView extends StatelessWidget {
  const CompleteView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("お疲れ様でした！", style: TextStyle(fontSize: 22)),
    );
  }
}
